# Canonical agent workflow:
#   .codex/skills/deploy/SKILL.md
#
# Release checklist:
# 1. Update affected docs, examples, and CHANGELOG.
# 2. Bump VERSION and DATE_BUILD in src/STest.php.
# 3. Update the exact --version assertion in src/test/exit-status.stest.
# 4. Run PHP/Bash/ShellCheck/Composer validation and bin/stest-all -q src/test.
# 5. Review the complete diff and require a clean, understood release scope.
# 6. Commit as "Release VERSION" and create an annotated VERSION tag.
# 7. Push main and the tag.
# 8. Verify GitHub refs and Packagist source metadata.
# 9. Deploy named hosts with .codex/skills/deploy/scripts/deploy.sh.
# 10. Synchronize the standard fleet with $sync-versions when requested.

git tag -a VERSION -m "VVERSION - Release summary"
git push origin main
git push origin VERSION

# Packagist updates through the GitHub webhook. Verify:
# https://repo.packagist.org/p2/parf/spartan-test.json
